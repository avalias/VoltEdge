[package]
name = "voltedge_attestor"
edition = "2024.beta"
version = "0.0.2"

# Built against the live DeepBook Predict protocol (deepbook + deepbook_predict
# sit alongside as siblings, linked to their deployed testnet addresses).
[dependencies]
deepbook_predict = { local = "../predict" }
deepbook = { local = "../deepbook" }

[addresses]
voltedge_attestor = "0x0"
